
export interface Project {
  id: string;
  title: string;
  year: string;
  image: string;
  category: string;
}

export interface Service {
  title: string;
  description: string;
}

export interface Stage {
  number: string;
  title: string;
  description: string;
}
